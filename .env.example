# Copy to .env.local and fill in. Do not commit real secrets.

# Optional: app Postgres cache (secrets list TTL)
# DATABASE_URL=postgres://user:pass@localhost:5432/flightdeck_cache

# AWS / UI
# NEXT_PUBLIC_AWS_PROFILES=
